import { SearchOptionsPipe } from './search-options.pipe';

describe('SearchOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
